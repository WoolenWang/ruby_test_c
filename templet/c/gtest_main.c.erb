/**
# Author: Woolen.Wang
# Email: just_woolen＠qq.com
# File Name: gtest_main.c
=========================================================================
 Description: 
    这个是一个Gtest的C源程序测试入口模板
 Edit History: 
   2014-01-07    File created.
=========================================================================
**/
#include "gtest/gtest.h"
<%=
     include_str = ""
     @test_include_files.each do |include_file|
         include_str << "#include \"#{include_file}\"\n"
     end
     include_str
%>

<%=
     before_action_str = ""
     @test_before_actions.each do |before_action|
#TODO 需要增加before action的解析部分
        before_action_str<< "// \"#{before_action}\"\n"
     end
     before_action_str
%>
<%=
     user_case_str = ""
     @test_user_cases.each do |user_case|
#TODO 需要增加before action的解析部分
         user_case_str << "// \"#{user_case}\"\n"
     end
     user_case_str
%>
int main(int argc, char** argv)
{
        ::testing::InitGoogleTest(&argc, argv);
        return RUN_ALL_TESTS();
}

